[project]
name = "calculadora"
version = "0.1.0"
description = "Servidor MCP de calculadora"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp==1.4.1"
]

[project.optional-dependencies]
dev = ["debugpy==1.8.8", "numpy==2.3.3"]